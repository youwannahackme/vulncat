# vulncat
Vulncat is a professional-grade, Go-based vulnerability-surface URL classifier for bug bounty hunters. It replaces flat regex with multi-signal heuristics, value-shape analysis, and REST API context to accurately categorize endpoints for SQLi, XSS, SSRF, LFI, and IDOR while filtering tracking noise via a fast, concurrent streaming pipeline.
